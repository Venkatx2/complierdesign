%{
#include<stdio.h>
#include<ctype.h>
%}

%%
[a-z]+  {
            int i;
            for(i=0; yytext[i] != '\0'; i++)
                yytext[i] = toupper(yytext[i]);
            printf("%s", yytext);
        }

\n      { printf("\n"); }

.       { printf("%s", yytext); }
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter the string:\n");
    yylex();
    return 0;
}